# Test

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 14.1.0.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The application will automatically reload if you change any of the source files.

## Used packages
Angular Material: offers reusable and beautiful UI components (Buttons, GridList, Tabs, Inputs, Icons, Dialog ...)

## Login/Logout Sessions
Controlled by auth.guards

## Responsive View
Bootstrap & css media type

## Note
concerning the posts there is no uri images path in the api 
